//一层优化strcpy函数
#include <stdio.h>
void my_strcpy(char* des, char* sou)
{
	while (*des++=*sou++)//利用后置加加 以及 \0 为假
	{
		;
	}
	
}
int main()
{
	char arr1[] = { "********" };
	char arr2[] = { "hello" };
	my_strcpy(arr1, arr2);
	printf("%s", arr1);
	return 0;
}
